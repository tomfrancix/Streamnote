@model Streamnote.Relational.Models.Item

@{
    ViewData["Title"] = "Edit";
}


<form asp-action="CreateOrUpdate" enctype="multipart/form-data">
    @Html.Partial("_Editor", Model)
</form>
<div>
    <a asp-action="Index">Back to List</a>
    <a class="deleteItem" data-iden="@Model.Id">Delete</a>
</div>
